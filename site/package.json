{
  "name": "uploader-site",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "start": "yarn build",
    "dev": "parcel src/index.html",
    "build": "rm -rf dist && parcel build src/index.html --no-source-maps",
    "publish": "yarn run build && now --prod"
  },
  "devDependencies": {
    "@types/node": "^14.0.6",
    "@types/react": "^16.9.17",
    "@types/react-dom": "^16.9.4",
    "less": "^3.11.2",
    "parcel-bundler": "^1.12.4"
  },
  "dependencies": {
    "easy-uploader.js": "^2.0.0",
    "react": "^16.12.0",
    "react-dom": "^16.12.0",
    "spark-md5": "^3.0.1",
    "typescript": "^3.7.5"
  }
}
